site:
  title: Notesbase Docs
  # Set this to your GitHub Pages URL once known, e.g.:
  # url: https://<owner>.github.io/notesbase

content:
  sources:
    - url: .
      # 'HEAD' always includes the currently checked-out branch.
      # Add branch names to include additional branches, e.g. [HEAD, dev].
      branches: [ HEAD, main ]
      # Tags are injected at build time by the CI workflow — only tags that
      # contain docs/antora.yml are included. See .github/workflows/docs.yml.
      start_path: docs
      version: true

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

output:
  dir: ./build/site
